knitr::opts_chunk$set(echo = TRUE)
# First we import the data
walkability <- read_csv("./data/EPA_SmartLocationDatabase_V3_Jan_2021_Final.csv")
library(tidyverse)
knitr::opts_chunk$set(echo = TRUE)
library(tidyverse)
# First we import the data
walkability <- read_csv("./data/EPA_SmartLocationDatabase_V3_Jan_2021_Final.csv")
colleges <- read_csv("./data/colleges.csv") %>% rename(city = CITY)
head(walkability, 5)
head(colleges, 5)
# Filter colleges to only include "larger" colleges. To do this, we use the Carnegie Size metric. TODO: I think this classifies colleges based on size and a couple other metrics, and it seems like it is a pretty good way to differentiate small universities and bigger universities that we can call "college towns".
colleges <- colleges %>% filter(CARNEGIESIZE >= 4) %>%
select(INSTNM, city)
# We group walkability by city and take the average for each city
city_walkability <- walkability %>% group_by(city) %>% summarise(walkability = mean(walkability))
city_walkability
# We add a boolean to see if a city has a large college.
joined_data <- city_walkability %>%
mutate(has_college = city %in% colleges$city)
# First we import the data
walkability <- read_csv("./data/EPA_SmartLocationDatabase_V3_Jan_2021_Final.csv")
# First we import the data
walkability <- read_csv("./data/EPA_SmartLocationDatabase_V3_Jan_2021_Final.csv")
colleges <- read_csv("./data/colleges.csv") %>% rename(city = CITY)
# Clean data by keeping only city and walkability columns
# To get cities, we split the CSA_Name column to only include characters up to the first '-' which is the name of the city.
walkability <- walkability %>%
select(city = CSA_Name, walkability = NatWalkInd) %>% mutate(city = str_split(city, "-", simplify = TRUE)[,1])
# Filter colleges to only include "larger" colleges. To do this, we use the Carnegie Size metric. TODO: I think this classifies colleges based on size and a couple other metrics, and it seems like it is a pretty good way to differentiate small universities and bigger universities that we can call "college towns".
colleges <- colleges %>% filter(CARNEGIESIZE >= 4) %>%
select(INSTNM, city)
# We group walkability by city and take the average for each city
city_walkability <- walkability %>% group_by(city) %>% summarise(walkability = mean(walkability))
city_walkability
# We add a boolean to see if a city has a large college.
joined_data <- city_walkability %>%
mutate(has_college = city %in% colleges$city)
write.csv(joined_data, "clean.csv", row.names = FALSE)
write.csv(joined_data, "./data/clean.csv", row.names = FALSE)
knitr::opts_chunk$set(echo = TRUE)
bm
bm <- read_csv("../Data/bm")
bm <- read_csv("../Data/bm.csv")
bm
xbar = 223.8312
n = 6448
cv = qt(c+(1-c)/2, n-1) # for this example qnorm(c+(1-c)/2) is almost the same as the cv we have here because our sample size is huge
cv
Is the true average time taken to finish the Boston Marathon equal to 4 hrs (240 mins)?
se
se
se = s/sqrt(n)
se
se = s/sqrt(n)
bm
bm_summary = bm %>%
filter(age_range == "18-34", Year == 2010) %>%
summarize(averageTime = mean(total_time), sdTime = sd(total_time), n = n())
xbar = 223.8312
s = 40.90398
n = 6448
# 95% CI for the true average time taken to complete BM.
c = 0.95
cv = qt(c+(1-c)/2, n-1) # for this example qnorm(c+(1-c)/2) is almost the same as the cv we have here because our sample size is huge
cv
se = s/sqrt(n)
se
xbar + c(-1, 1)*cv*se
mu_h0 = 240
t = (xbar-mu_h0)/(s/sqrt(n))
t
p_val = 2*pt(abs(t), n-1, lower.tail = F)
p_val # alpha = 0.05, we reject H_0
times = bm %>% filter(age_range == "18-34" & Year == 2010) %>% pull(total_time)
t.test(times) # H_0: mu = 0
t.test(times, mu = 240, alternative = "less", conf.level = 0.9) # mu is for changing the val for hypothesis test, alternative is for changing the alternative hypo (by default it is two sided), conf.level = to change the confidence level of confidence interval
write.csv(joined_data, "./Data/clean.csv", row.names = FALSE)
knitr::opts_chunk$set(echo = TRUE)
library(tidyverse)
# First we import the data
walkability <- read_csv("./data/EPA_SmartLocationDatabase_V3_Jan_2021_Final.csv")
colleges <- read_csv("./data/colleges.csv") %>% rename(city = CITY)
# Clean data by keeping only city and walkability columns
# To get cities, we split the CSA_Name column to only include characters up to the first '-' which is the name of the city.
walkability <- walkability %>%
select(city = CSA_Name, walkability = NatWalkInd) %>% mutate(city = str_split(city, "-", simplify = TRUE)[,1])
# Filter colleges to only include "larger" colleges. To do this, we use the Carnegie Size metric. TODO: I think this classifies colleges based on size and a couple other metrics, and it seems like it is a pretty good way to differentiate small universities and bigger universities that we can call "college towns".
colleges <- colleges %>% filter(CARNEGIESIZE >= 4) %>%
select(INSTNM, city)
# First we import the data
walkability <- read_csv("./data/EPA_SmartLocationDatabase_V3_Jan_2021_Final.csv")
colleges <- read_csv("./data/colleges.csv") %>% rename(city = CITY)
# Filter colleges to only include "larger" colleges. To do this, we use the Carnegie Size metric. TODO: I think this classifies colleges based on size and a couple other metrics, and it seems like it is a pretty good way to differentiate small universities and bigger universities that we can call "college towns".
colleges <- colleges %>% filter(INSTSIZE >= 3) %>%
select(INSTNM, city)
# We group walkability by city and take the average for each city
city_walkability <- walkability %>% group_by(city) %>% summarise(walkability = mean(walkability))
# First we import the data
walkability <- read_csv("./data/EPA_SmartLocationDatabase_V3_Jan_2021_Final.csv")
# First we import the data
walkability <- read_csv("./data/EPA_SmartLocationDatabase_V3_Jan_2021_Final.csv")
colleges <- read_csv("./data/colleges.csv") %>% rename(city = CITY)
head(walkability, 5)
head(colleges, 5)
# Clean data by keeping only city and walkability columns
# To get cities, we split the CSA_Name column to only include characters up to the first '-' which is the name of the city.
walkability <- walkability %>%
select(city = CSA_Name, walkability = NatWalkInd) %>% mutate(city = str_split(city, "-", simplify = TRUE)[,1])
# Filter colleges to only include "larger" colleges. To do this, we use the Carnegie Size metric. TODO: I think this classifies colleges based on size and a couple other metrics, and it seems like it is a pretty good way to differentiate small universities and bigger universities that we can call "college towns".
colleges <- colleges %>% filter(INSTSIZE >= 3) %>%
select(INSTNM, city)
# We group walkability by city and take the average for each city
city_walkability <- walkability %>% group_by(city) %>% summarise(walkability = mean(walkability))
city_walkability
# We add a boolean to see if a city has a large college.
joined_data <- city_walkability %>%
mutate(has_college = city %in% colleges$city)
write.csv(joined_data, "./Data/clean.csv", row.names = FALSE)
joined_data %>% filter(has_college == T) %>% count())
joined_data %>% filter(has_college == T) %>% count()
joined_data %>% filter(has_college == F) %>% count()
joined_data %>% filter(has_college == T) %>% count()
joined_data %>% filter(has_college == F) %>% count()
# Filter colleges to only include "larger" colleges. To do this, we use the Carnegie Size metric. TODO: I think this classifies colleges based on size and a couple other metrics, and it seems like it is a pretty good way to differentiate small universities and bigger universities that we can call "college towns".
colleges <- colleges %>% filter(INSTSIZE >= 4) %>%
select(INSTNM, city)
# First we import the data
walkability <- read_csv("./data/EPA_SmartLocationDatabase_V3_Jan_2021_Final.csv")
colleges <- read_csv("./data/colleges.csv") %>% rename(city = CITY)
head(walkability, 5)
head(colleges, 5)
# Clean data by keeping only city and walkability columns
# To get cities, we split the CSA_Name column to only include characters up to the first '-' which is the name of the city.
walkability <- walkability %>%
select(city = CSA_Name, walkability = NatWalkInd) %>% mutate(city = str_split(city, "-", simplify = TRUE)[,1])
# Filter colleges to only include "larger" colleges. To do this, we use the Carnegie Size metric. TODO: I think this classifies colleges based on size and a couple other metrics, and it seems like it is a pretty good way to differentiate small universities and bigger universities that we can call "college towns".
colleges <- colleges %>% filter(INSTSIZE >= 4) %>%
select(INSTNM, city)
# We group walkability by city and take the average for each city
city_walkability <- walkability %>% group_by(city) %>% summarise(walkability = mean(walkability))
city_walkability
# We add a boolean to see if a city has a large college.
joined_data <- city_walkability %>%
mutate(has_college = city %in% colleges$city)
joined_data %>% filter(has_college == T) %>% count()
joined_data %>% filter(has_college == F) %>% count()
write.csv(joined_data, "./Data/clean.csv", row.names = FALSE)
joined_data %>% filter(has_college == T) %>% count()
joined_data %>% filter(has_college == F) %>% count()
# Filter colleges to only include "larger" colleges. To do this, we use the Carnegie Size metric. TODO: I think this classifies colleges based on size and a couple other metrics, and it seems like it is a pretty good way to differentiate small universities and bigger universities that we can call "college towns".
colleges <- colleges %>% filter(INSTSIZE >= 5) %>%
select(INSTNM, city)
# First we import the data
walkability <- read_csv("./data/EPA_SmartLocationDatabase_V3_Jan_2021_Final.csv")
colleges <- read_csv("./data/colleges.csv") %>% rename(city = CITY)
head(walkability, 5)
head(colleges, 5)
# Clean data by keeping only city and walkability columns
# To get cities, we split the CSA_Name column to only include characters up to the first '-' which is the name of the city.
walkability <- walkability %>%
select(city = CSA_Name, walkability = NatWalkInd) %>% mutate(city = str_split(city, "-", simplify = TRUE)[,1])
# Filter colleges to only include "larger" colleges. To do this, we use the Carnegie Size metric. TODO: I think this classifies colleges based on size and a couple other metrics, and it seems like it is a pretty good way to differentiate small universities and bigger universities that we can call "college towns".
colleges <- colleges %>% filter(INSTSIZE >= 5) %>%
select(INSTNM, city)
# We group walkability by city and take the average for each city
city_walkability <- walkability %>% group_by(city) %>% summarise(walkability = mean(walkability))
city_walkability
# We add a boolean to see if a city has a large college.
joined_data <- city_walkability %>%
mutate(has_college = city %in% colleges$city)
joined_data %>% filter(has_college == T) %>% count()
joined_data %>% filter(has_college == F) %>% count()
write.csv(joined_data, "./Data/clean.csv", row.names = FALSE)
joined_data %>% filter(has_college == T) %>% count()
joined_data %>% filter(has_college == F) %>% count()
# First we import the data
walkability <- read_csv("./data/EPA_SmartLocationDatabase_V3_Jan_2021_Final.csv")
head(walkability, 5)
